name: company_scheduler
description: Freelance project for Nikola D

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  http: ^0.12.2
  shared_preferences: ^0.5.12+4
  url_launcher: ^5.7.10 
  firebase_messaging: ^7.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/dash/